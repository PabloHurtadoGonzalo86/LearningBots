apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: learning-bots

resources:
  - namespace.yaml
  - configmap.yaml
  # Copy secret.example.yaml to secret.yaml and fill with real values
  # Then uncomment the line below:
  # - secret.yaml
  - api-deployment.yaml
  - api-service.yaml
  - web-deployment.yaml
  - web-service.yaml
